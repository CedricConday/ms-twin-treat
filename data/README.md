# data/

Loaders + backtest anchors. **Open datasets only — no raw patient data committed to the repo.** Loaders fetch from source (Zenodo/GEO) and cache locally, gitignored. The v0 anchor is Kang GSE96583 (IFN-β on PBMCs). Clinical-layer anchors (relapse/EDSS/MRI) come from MSOAC + published trials. Full data map: `ms-twin/docs/RESEARCH_FINDINGS.md`. Empty until v0.
