# Dev/test deps (on top of requirements.txt). Runtime does not need these.
-r requirements.txt
pytest==9.1.1
matplotlib==3.11.1   # results/figures.py (deck assets)
ruff==0.15.20        # lint gate; see pyproject.toml [tool.ruff]
