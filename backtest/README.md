# backtest/

The validation harness — the edge of the whole project. Trust nothing until it replays known history. Scores whether the sim reproduces a *known* published outcome before any prediction is believed. Modeled on perturbation-prediction benchmarks (scArchon-style). This is what makes a claim credible instead of a demo that looks alive but predicts nothing. Empty until v0.
