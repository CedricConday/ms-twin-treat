# Pinned coherent stack — avoids the numpy 2.4 / pandas ABI break on this box.
# v0 harness is pure numpy/scipy/pandas/anndata — no GPU, no torch required.
numpy==2.2.6
scipy==1.14.1
pandas==2.2.3
scikit-learn==1.5.2
anndata==0.11.3
