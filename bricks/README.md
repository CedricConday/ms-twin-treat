# bricks/

Adapters to component models — other people's models, our thin wrappers. One subdir per brick (cell/, population/, barrier/). Each wrapper pins the upstream version + license and exposes a uniform interface to the spine. No brick logic reimplemented here; we adapt, not fork. Empty until v0.
